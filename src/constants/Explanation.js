const MAIN = "Please enter full URI, include the \"https\" part.";

const BUBBLE_GRAPH = "Click to check MDN search results!";

const COMPAT = "Check browser compatibilities!";

const COLOR = "Drag to make your own color theme. Click to copy color code!";

const EXPLANATION = {
  MAIN,
  BUBBLE_GRAPH,
  COMPAT,
  COLOR,
};

export default EXPLANATION;
